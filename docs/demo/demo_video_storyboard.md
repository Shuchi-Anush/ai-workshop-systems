# YouTube / GitHub Demo Storyboard

**Format:** Fast-paced, screen-recording only, zero-fluff technical demo.
**Length:** 120 Seconds
**Music:** Minimalist lo-fi electronic (low volume).

| Timestamp | Visual | Audio/Text Overlay |
| :--- | :--- | :--- |
| **0:00 - 0:05** | Split screen: VS Code Monorepo on left, terminal running `start_platform.bat` on right. | *Text:* "Local-First AI Resume Intelligence. 8GB RAM limit. Offline." |
| **0:05 - 0:15** | Streamlit Dashboard opens. Navigates to `Live Health` tab showing Ollama models active, indexes synced. | *Text:* "Operationally hardened. Zero cloud dependencies." |
| **0:15 - 0:35** | `Attack Simulator` tab. Types "Senior React Engineer". Clicks "Simulate Dense-Only". Red box flashes `ADVERSARIAL LEAK`. | *Text:* "The Problem: Vector databases are vulnerable to keyword stuffing. Semantic contamination overrides legitimate resumes." |
| **0:35 - 0:55** | Clicks "Simulate Hybrid (Protected)". The adversarial resume drops. Green boxes flash `Clean Candidate`. | *Text:* "The Solution: Reciprocal Rank Fusion (BM25 + ChromaDB) paired with NLP Adversarial Heuristics." |
| **0:55 - 1:10** | `1-Click Executive Demo` tab. Expands a candidate card to show the `Explainability Trace`. | *Text:* "Deterministic Explainability. Proving why a document was retrieved without slow LLM generation." |
| **1:10 - 1:20** | Terminal window. Runs `uv run python apps/resume-analyzer/scripts/production_simulator.py`. Metrics fly by showing `<50ms` latency. | *Text:* "Production Stress Tested. 100 concurrent queries. 50ms SLA." |

**Final Slide:** Link to GitHub repository. `github.com/yourname/ai-workshop-systems`
